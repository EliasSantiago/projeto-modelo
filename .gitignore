# dependencies
/node_modules
.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/playwright-report
/test-results

# drizzle
# ATENÇÃO: /drizzle (incluindo /drizzle/meta) É versionado de propósito.
# O journal em meta/ é o que permite ao drizzle-kit gerar migrations
# incrementais corretas. Ignorá-lo quebra o histórico de schema.

# env, nunca commitar secrets
.env
.env*.local

# misc
.DS_Store
*.pem
*.tsbuildinfo
